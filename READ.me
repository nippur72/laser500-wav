# laser500-wav

Converts a Laser 500 executable program into a tape-loadable WAV file (with optional TURBO tape).


## Installation

```
npm i -g laser500-wav
```

## Usage

After installation, you can call the command `laser500wav` from the prompt.

Example:

```
laser500wav -i amsterd.bin -o amsterd.wav --turbo-tape
```

